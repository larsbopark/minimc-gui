from distutils.core import setup

setup(name='minimc',
      version='${PACKAGE_VERSION}',
      package_dir={ 'minimc': '${CMAKE_CURRENT_SOURCE_DIR}/app' },
      package_data={"minimc" : ["${CMAKE_CURRENT_BINARY_DIR}/app/*.so"] },
      packages=['minimc'])
